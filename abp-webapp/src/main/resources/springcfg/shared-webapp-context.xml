<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-2.5.xsd">

    <bean class="com.aircell.abp.utils.AircellServletUtils"
          lazy-init="true">
        <property name="libraryPath" value="/${static-webapp.path}"/>
        <property name="gogolibraryPath" value="/${gogo-static-webapp.path}"/>
        <property name="ipHeaderName" value="X-Forwarded-For"/>
    </bean>

    <!-- *** Velocity configuration *** -->
    <!-- configures internal of Velocity. this bean is only used internally by velocity -->
    <bean id="velocityEngine"
          class="org.springframework.ui.velocity.VelocityEngineFactoryBean">
        <property name="velocityProperties">
            <props>
                <prop key="resource.loader">class</prop>
                <prop key="class.resource.loader.class">
                    org.apache.velocity.runtime.resource.loader.ClasspathResourceLoader
                </prop>
            </props>
        </property>
    </bean>

    <bean id="velocityConfig"
          class="org.springframework.web.servlet.view.velocity.VelocityConfigurer">
        <property name="resourceLoaderPath"
                  value="/WEB-INF/velocity-templates/"/>
        <property name="velocityProperties">
            <props>
                <prop key="velocimacro.library">velocity/macros.vm</prop>
            </props>
        </property>
    </bean>

</beans>